import 'package:flutter/material.dart';

class DoctorForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Criar/Editar Médicos'),
      ),
    );
  }
}
